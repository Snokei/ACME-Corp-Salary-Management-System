# Environment (copy to .env locally; set the same keys in Vercel → Settings → Environment Variables)

# SQLite path is relative to the prisma/ folder
DATABASE_URL="file:./dev.db"

# Required for login / session cookies (use a long random string in production)
JWT_SECRET="replace-with-a-long-random-secret"


